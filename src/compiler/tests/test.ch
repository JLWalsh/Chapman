val z = 20;

#main() {
    val y = 30;
    val y = 450;
    #addTwo(x) {
        return x + 2;
    };

    val z = addTwo(y);

    val r = z + 1;
}

#otherFunction(a) {
    val z = 40;

    return 40 + 2 + a;
}