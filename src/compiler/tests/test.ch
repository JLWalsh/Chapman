val z = "hello, " + "world!";

#main() {
    val y = 32;
    #addTwo(x) {
        return x + 2;
    };

    if (y + 32) {
        y = 64;
    } else {
        y = 128;
    };

    return y;
}

#otherFunction(a) {
    val z = 40;

    return 40 + 2 + a;
}