#main() {
    for(val x = 0; x < 10; x++) {
        print("value is " + x);
    }

    val y = 5;
    for(; y < 10; y++) {
        print("value is " + y);
    }

    print("done looping");
}

